name            = "taylorhelmantattoo-availability"
main            = "worker.js"
compatibility_date = "2024-09-23"

# ── Public variables (committed to Git — not sensitive) ───────────────────────
[vars]
CALENDAR_ID     = "08637d77770857f7ad5f19feb231fced19a2aec3d58026036825aea0140e3949@group.calendar.google.com"
TIMEZONE        = "America/Toronto"
ALLOWED_ORIGIN  = "https://taylorhelmantattoo.com"
BOOKING_HORIZON = "60"
LEAD_TIME_HOURS = "48"

# ── Secret (set via CLI — NEVER add to this file) ─────────────────────────────
# Run once after cloning:
#   npx wrangler secret put GOOGLE_SERVICE_ACCOUNT_JSON
# Paste the full contents of your Google service account .json key file.

[dev]
port           = 8787
local_protocol = "http"
